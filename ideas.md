# Ideas

- CI speed follow-ups parked for approval (worker-B, P11.8): scope
  Windows `dist generate --check` to ubuntu (loses per-OS P6.3
  proof); combine the two NPU `cargo check`s (loses separate
  hexagon/openvino resolve proof); sccache with measurements
  (cold-cache physics: ~8.5-12 min without it).
