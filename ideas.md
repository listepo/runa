# Ideas

- P10 batch closed P8.7 follow-ups (calibration wiring, `--threads`,
  `--lang auto` fix, reasoning counts, serve idle tick); remaining:
  quiet-machine re-runs (M5 TTFT, M7 60 s timing, M8 audio, M4 8B) +
  release build for M10 size + Metal build for real M4/M5 gates.
