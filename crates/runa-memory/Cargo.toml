[package]
name = "runa-memory"
description = "adaptive memory manager (idle shrink / heavy grow) + task-claim registry"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[dependencies]
chrono = "0.4"
sysinfo = "0.39"

[dev-dependencies]
tempfile = "3"
